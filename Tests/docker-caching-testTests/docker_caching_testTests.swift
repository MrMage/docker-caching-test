import XCTest
@testable import docker_caching_test

final class docker_caching_testTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(docker_caching_test().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
